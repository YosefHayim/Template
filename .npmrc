# Save exact versions (no ^ or ~ prefix)
save-exact=true

# Use the lockfile
package-lock=true

# Enforce Node.js version from engines field
engine-strict=true

# Enforce strict peer dependency resolution (npm 7+ installs peers by default)
legacy-peer-deps=false
